<!DOCTYPE html>
<html>
<head>
  <title>Quotesapp - Read quotes from great authors and share with your friends</title>
  <%= stylesheet_link_tag    "application",  :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="framecontentLeft">
   <div class="innertube">
  </div>
</div>

<div id="framecontentRight">
  <div class="innertube">

  </div>
</div>


<div id="maincontent">
  <img src='<%=asset_path "quote_header.jpg"%>' width="1200" height='100' />
   <table><tr><td>
           <% unless isLoggedIn? %>
              <%= link_to image_tag('login-facebook-button.png'), :fblogin %>
           <% else %>
              <span class="label">Logged in facebook as: <%= currentUser %></span>
           <% end %> </td></tr>
           <tr><td>
           <% unless isLoggedInTwitter? %>
	       <%= link_to image_tag('login-twitter-button.png'), :twitterlogin %>
	   <% else %>
	       <span class="label">Logged in twitter as: <%= currentTwitterUser %></span>
           <% end %> 
           </td></tr></table>
  <div class="innertube">
   <%= yield %>
  </div>
</div>

</body>
</html>
